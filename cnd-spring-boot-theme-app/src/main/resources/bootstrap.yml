spring:
  application:
    name: cnd-spring-boot-theme-app
  cloud:
    config:
      uri: http://localhost:8888/
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: "*"
